# Docker Environment Configuration for Restaurant Management System

# Server Configuration
NODE_ENV=production
PORT=5000

# MongoDB Connection (Docker internal network)
MONGO_URI=mongodb://mongodb:27017/restaurant_db

# JWT Secret Key
# IMPORTANT: Change this to a secure random string for production
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=18d9b5f0ce341e074c52ef48db4d522bcc78822bacddc11d202302c4f2112c07

# Client URL (nginx proxy)
CLIENT_URL=http://localhost

# Socket.IO Configuration
CORS_ORIGIN=http://localhost
